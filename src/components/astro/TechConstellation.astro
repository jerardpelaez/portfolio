---
import TechConstellationVue from '../vue/TechConstellation.vue'
import { positionedSkills, skillCategories } from '../../data/skills'
---

<section id="skills" class="py-20 md:py-32 bg-[#10120F] relative overflow-hidden">
  <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 relative z-10">
    <!-- Header -->
    <div class="text-center mb-16 reveal">
      <span class="inline-block text-[#A78BFA] font-semibold mb-4">My Skills</span>
      <h2 class="text-3xl sm:text-4xl md:text-5xl font-bold text-[#F0EDE8] mb-6">
        Technologies I Work With
      </h2>
      <p class="text-lg text-[#9A9990] max-w-2xl mx-auto">
        I continuously expand my skill set to stay ahead in the ever-evolving tech landscape.
      </p>
    </div>

    <!-- Skills Display -->
    <div class="reveal">
      <TechConstellationVue
        client:visible
        skills={positionedSkills}
        categories={skillCategories}
      />
    </div>
  </div>
</section>
